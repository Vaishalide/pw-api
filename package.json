{
  "name": "jwt-proxy-server",
  "version": "1.0.0",
  "description": "A stateless, scalable proxy server using JWT for temporary stream access.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "proxy",
    "jwt",
    "stream",
    "hls",
    "dash",
    "heroku"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  }
}
